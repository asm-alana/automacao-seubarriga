package me.wcaquino.core.setup;

public class Propriedades {

public static boolean FECHAR_BROWSE = true;

    public static Browsers browser = Browsers.CHROME;

    public enum Browsers {
        CHROME,
        FIREFOX,
        EDGE,
        HEADLESS
    }
}
